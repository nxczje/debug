{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Attach to Subdir",
            "type": "node",
            "request": "attach",
            "port": 9229,
            "address": "localhost",
            "localRoot": "${workspaceFolder}/src",
            "remoteRoot": "/node/src",
            "protocol": "inspector"
        }
    ]
}